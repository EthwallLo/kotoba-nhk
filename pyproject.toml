[project]
name = "kotoba-nhk-news"
version = "0.1.0"
description = "Retrieve the current NHK News Web article list."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.scripts]
nhk-articles = "nhk_articles.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
